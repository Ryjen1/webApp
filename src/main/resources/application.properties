# # Spring Boot Application Name
# spring.application.name=webApp

# # PostgreSQL Database Configuration
# spring.datasource.url=jdbc:postgresql://dpq-d0flo8i4d50c73f1n6l0-a.postgres.app:5432/webapp_db_kx2h
# spring.datasource.username=webapp_db_kx2h_user
# spring.datasource.password=xHvAlToair9HHDLkqpbhSkPDviTLOD9m
# spring.datasource.driver-class-name=org.postgresql.Driver

# # Hibernate Settings
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.properties.hibernate.jdbc-timeout=20000

# # Optional: Disable H2 Console (if used locally)
# spring.h2.console.enabled=false

# Spring Boot Application Name
spring.application.name=webApp

# Hibernate Settings
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Disable H2 Console (if used locally)
spring.h2.console.enabled=false